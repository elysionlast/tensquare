server:
  port: 8001
spring:
  application:
    name: tensquare-base
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql:///tensquare_base?characterEncoding=utf-8
  jpa:
    database: mysql
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
  instance:
    prefer-ip-address: true